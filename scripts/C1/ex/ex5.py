def placer(x, t):
    """
    Place x dans le tableau trié t. Renvoie un nouveau tableau
    """
    if ...:
        return [x]
    elif x < t[0]:
        return [x] + ...
    else:
        return [t[0]] + ...